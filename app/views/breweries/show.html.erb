<h2>
  <%= @brewery.name %>
</h2>

<p>
  <em>Established at
    <%= @brewery.year %>
  </em>
</p>

<p>
  Number of beers
  <%= @brewery.beers.count %>
</p>

<p>
  <% @brewery.beers.each do |beer| %>
    <%= link_to beer.name, beer %>
  <% end %>
</p>

<p> <% if @brewery.ratings.empty? %>
  Brewery has no ratings for any beers!
<% else %>
  has <%=pluralize(@brewery.ratings.count, 'rating') %>, average <%= @brewery.average_rating %>
<% end %>
</p>

<%= link_to 'Edit', edit_brewery_path(@brewery) if current_user %> |
<%= link_to 'Back', breweries_path %>

<!-- <%= link_to "List of Beers", beers_path %> -->
